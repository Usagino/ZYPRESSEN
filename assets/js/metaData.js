export default function metaData(host) {
  const data = {
    description: `ZYPRESSEN is a creative team that creates digital product designs. We don't believe in stereotypes, and each member of our team is always working with a cutting-edge philosophy. In order to create something new and different, we observe and absorb what our predecessors have built and find new values that have never been seen before.
We call this "Different from Others".`,
    image: host + 'OGP.png',
    title: 'ZYPRESSEN',
    url: host,
  }

  return { data }
}
